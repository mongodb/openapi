extends:
  - ./rulesets/IPA-104.yaml

rules:
  xgen-find-exceptions:
    description: Find components with x-xgen-IPA-exception
    severity: info
    given: $..[?(@["x-xgen-IPA-exception"])]
    then:
      function: truthy