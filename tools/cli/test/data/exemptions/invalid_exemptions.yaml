# Invalid exemptions 
# Exemptions are valid if they have the following fields:
- "breaking_change_description": "removed the schema 'GroupName' [api-schema-removed]"
  "hide_from_changelog": "true"
- "breaking_change_description": "GET /api/atlas/v2/groups/{groupId}/clusters/provider/regions removed the 'GSP' enum value from the 'results/items/provider' response property for the response status '200' [response-property-enum-value-removed]."
  "exempt_until": "3000-03-27"
  "hide_from_changelog": "true"
- "breaking_change_description": "GET /api/atlas/v1.0/groups/{groupId}/clusters/provider/regions removed the 'GSP' enum value from the 'results/items/provider' response property for the response status '200' [response-property-enum-value-removed]."
  "reason": "spec correction"
  "hide_from_changelog": "true"
  