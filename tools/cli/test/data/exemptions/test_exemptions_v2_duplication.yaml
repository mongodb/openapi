- breaking_change_description: "API PATCH /api/atlas/v1.0/testGroups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId} removed the 'replicaSet' enum value from the 'type' response property for the response status '200' [response-property-enum-value-removed]."
  exempt_until: "2100-06-14"
  reason: "spec correction"
- breaking_change_description: "API POST /api/atlas/v2/testGroups/{groupId}/clusters/{clusterName}/backup/snapshots removed the 'replicaSet' enum value from the 'type' response property for the response status '200' [response-property-enum-value-removed]."
  exempt_until: "2100-06-14"
  reason: "spec correction"
- breaking_change_description: "API GET /api/atlas/v2/testGroups/{groupId}/clusters/{clusterName}/backup/snapshots/shardedCluster/{snapshotId} removed the 'shardedCluster' enum value from the 'type' response property for the response status '200' [response-property-enum-value-removed]."
  exempt_until: "2100-06-14"
  reason: "spec correction"
  hide_from_changelog: "true"
