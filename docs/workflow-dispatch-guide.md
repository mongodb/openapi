# Manual Workflow Dispatch (Release Override)

When a spec change has been committed to a target branch but the automated
pipeline skipped the Bump.sh deployment, set `force_bump` to `true` when
dispatching. Without `force_bump`, a manual rerun regenerates the same spec
against the already-committed change, producing an empty diff, so the bump is
skipped again.

The bump check is at [`release-spec.yml:155-172`](https://github.com/mongodb/openapi/blob/main/.github/workflows/release-spec.yml#L155-L172).

## Triggering the Release Runner

1. Go to the **Actions** tab in the [mongodb/openapi](https://github.com/mongodb/openapi/actions) repository.
2. Select **"Release Runner: Start the Release Process for DEV, QA, STAGING and PROD"** from the workflow list on the left.
3. Click the **"Run workflow"** dropdown.
4. Choose the target environment from the `env_to_release` dropdown:

   |Value|Target Branch|Description|
   |---|---|---|
   |`dev`|`dev`|Development|
   |`qa`|`qa`|QA|
   |`staging`|`staging`|Staging|
   |`prod`|`main`|Production|
   |`dev,qa`|Both|Multi-env batch|
   |`dev,qa,staging`|All three|Multi-env batch|
   |`dev,qa,staging,prod`|All four|Full pipeline|

5. Set **`force_bump`** to `true` if the spec change is already committed and the
   original release skipped Bump.sh.
6. Click **"Run workflow"**.

The workflow also runs on a schedule (every 2 hours, Mon-Fri), which executes
**all environments** (dev, qa, staging, prod, plus the deprecated v1 spec for prod).

## Key Jobs to Monitor During the Rerun

Once the run starts, watch the following jobs in the workflow run summary:

### `release` job (`release-spec.yml`)

**`bump_changes` step** — determines whether Bump.sh deployment will proceed.

- When `force_bump` is `true`, `bump_release` is forced to `true` regardless of diff.
- Otherwise, the step checks both added and deleted lines in `openapi/v2.json`.
  If either exceeds 1, `bump_release=true`; this covers enum removals (>1 deleted
  line) while skipping x-xgen-sha-only updates (1 added + 1 deleted, neither > 1).
- To verify the outcome, check whether the **`release-bump-sh`** job ran (appears in
  the workflow run summary). If it is listed and succeeded, `bump_release` was `true`.

### `release-bump-sh` job (`generate-bump-pages.yml`)

This job only runs when `bump_release == 'true'`. It calls `bump-sh/github-action`
to deploy OpenAPI specs to Bump.sh for the target branch's documentation hub.

- Runs a matrix of spec files generated by `create-matrix` → `generateSpecMapping.js`.
- **`deploy-doc` step** confirms the actual deployment. Verify the logs show
  successful uploads for each spec in the matrix.

### `release-changelog` job (`release-changelog.yml`)

Generates and commits the API changelog by diffing the current spec against the
previous version.

- Confirm the **"Generate Changelog"** step runs without error.
- Verify a commit with message `ci(<env>): Release API Changelog` appears on the
  target branch.

## Verification After Successful Bump

1. **Bump.sh deployment**: Open the `release-bump-sh` job logs and confirm the
   `deploy-doc` step completed successfully for all spec files in the matrix.

2. **Published docs**: Navigate to the Bump.sh documentation URL for the target
   branch/version and confirm the spec reflects the expected change.

3. **Changelog entry**: Check `changelog/changelog.json` on the target branch — the
   change should appear as an entry with today's date.

## Environment-to-Branch Mapping

|Environment|Branch|S3 Bucket Variable|
|---|---|---|
|`dev`|`dev`|`S3_BUCKET_DEV`|
|`qa`|`qa`|`S3_BUCKET_QA`|
|`staging`|`staging`|`S3_BUCKET_STAGING`|
|`prod`|`main`|`S3_BUCKET_PROD`|